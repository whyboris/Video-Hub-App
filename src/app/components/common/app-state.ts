export let AppState = {
  selectedOutputFolder: '',
  selectedSourceFolder: '',
  currentView: 'thumbs', // can be 'thumbs' | 'filmstrip' | 'files'

  buttonsInView: true,

  numberToShow: 20,
  menuHidden: false,
  topHidden: false
}
